import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  itemContainer: {
    marginRight: 24,
    width: 180,
    marginBottom: 24,
  },
  title: {
    marginTop: 5,
    fontSize: 14,
    fontWeight: "bold",
  },
  thumbnail: {
    width: "100%",
    height: 112,
    borderRadius: 16,
  },
});
